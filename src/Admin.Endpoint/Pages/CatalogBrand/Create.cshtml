@page
@model CreateModel
@{
}


<div class="col-lg-12 col-12 layout-spacing">
    <div class="statbox widget box box-shadow">
        <div class="widget-header">
            <div class="row">
                <div class="col-xl-12 col-md-12 col-sm-12 col-12">
                    <h4> ثبت برند جدید  </h4>
                </div>
            </div>
        </div>
        <div class="widget-content widget-content-area">
            @foreach (var item in Model.Message)
            {
                <div class="alert alert-info">
                    <p>@item</p>
                </div>
            }
            <form asp-page="create" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group mb-4">
                    <label asp-for="CatalogBrand.Name"> </label>
                    <input type="text" class="form-control" asp-for="CatalogBrand.Name">
                    <span asp-validation-for="CatalogBrand.Name" class="text-danger"></span>
                </div>
                <input type="submit" name="time" value="ثبت" class="btn btn-primary">
                <a asp-page="index" class="btn btn-info">برگشت به لیست</a>
            </form>

        </div>
    </div>
</div>





@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}